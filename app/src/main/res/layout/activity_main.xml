<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- TextView com a versão do app (pequena, alinhada à direita ou centro) -->
    <TextView
        android:id="@+id/txtVersion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="2dp"
        android:gravity="center|center_horizontal|center_vertical|end"
        android:paddingBottom="2dp"
        android:text="@string/app_version"
        android:textAlignment="center"
        android:textColor="#888888"
        android:textSize="12sp"
        android:textStyle="bold" />

    <!-- Seleção de modo -->
    <RadioGroup
        android:id="@+id/radioGroupInputMode"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/radioHandwriting"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="true"
            android:text="Manuscrito"
            android:textSize="20sp"
            android:textStyle="bold" />

        <RadioButton
            android:id="@+id/radioKeyboard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:text="Teclado"
            android:textSize="20sp"
            android:textStyle="bold" />
    </RadioGroup>

    <!-- Área de entrada (manuscrito / teclado) -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:layout_weight="1">

        <com.example.handwritingtospeech.HandwritingView
            android:id="@+id/handwritingView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#FFFFFF" />

        <TextView
            android:id="@+id/txtNoHandwriting"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="Modo manuscrito não disponível neste dispositivo\n(Android 7 ou inferior)"
            android:textColor="#888888"
            android:textSize="18sp"
            android:visibility="gone" />

        <EditText
            android:id="@+id/editTextTyped"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#FFFFFF"
            android:gravity="top|start"
            android:hint="Digite o texto aqui"
            android:inputType="textMultiLine"
            android:padding="12dp"
            android:textSize="18sp"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Texto reconhecido -->

    <!-- Status da voz (permanece sempre visível) -->
    <TextView
        android:id="@+id/txtVoiceStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="#888888"
        android:gravity="center"
        android:padding="10dp"
        android:text="Status da voz"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Container SOMENTE para status do modelo (ProgressBar + texto) -->
    <LinearLayout
        android:id="@+id/modelStatusContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="vertical">

        <ProgressBar
            android:id="@+id/progressModel"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            android:visibility="gone" />

        <TextView
            android:id="@+id/txtModelStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/baixando_modelo"
            android:gravity="fill_horizontal"
            android:padding="12dp"
            android:textColor="#FFFFFF"
            android:background="#1976D2"
            android:layout_marginBottom="16dp"
            android:textAlignment="gravity"
            android:justificationMode="inter_word"/>

        <TextView
            android:id="@+id/txtRecognized"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="#EEEEEE"
            android:gravity="center"
            android:minHeight="84dp"
            android:padding="12dp"
            android:text=""
            android:textColor="#000000"
            android:textSize="20sp" />

    </LinearLayout>

    <!-- Botões lado a lado (sempre visíveis, fora do container) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center_horizontal"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnSpeak"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:background="@drawable/button_background"
            android:text="@string/falar"
            android:textColor="@android:color/white"
            android:textSize="20sp" />

        <Button
            android:id="@+id/btnClear"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/button_background_clear"
            android:text="@string/limpar"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold" />

    </LinearLayout>
</LinearLayout>